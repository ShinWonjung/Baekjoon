#include <iostream>
using namespace std;
#define LEN 1000001
char arr[LEN];

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	int m, n;
	cin >> m >> n;
	for (int i = m; i <= n; i++)
	{
		arr[i] = i;
	}
	for (int i = m; i <= n; i++)
	{
		if (arr[i] == 0) continue;
		for()
	}
	return 0;
}