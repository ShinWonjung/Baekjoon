#include <iostream>
#include <vector>

using namespace std;

int main()
{
	int n, m;
	cin >> n >> m;
	vector<int>vec{ 0 };
	for (int i = 1; i <= n; i++)
		vec.push_back(i);
	for (int i = 1; i <= n; i++)
	{
		for(int j=i)
	}
}